diff -Naurw firefox-42.0.orig/tools/profiler/lul/LulElf.cpp firefox-42.0/tools/profiler/lul/LulElf.cpp
--- firefox-42.0.orig/tools/profiler/lul/LulElf.cpp	2015-10-29 15:18:01.000000000 -0700
+++ firefox-42.0/tools/profiler/lul/LulElf.cpp	2015-11-04 12:26:35.540050166 -0800
@@ -484,9 +484,9 @@
 // last slash, or the whole filename if there are no slashes.
 string BaseFileName(const string &filename) {
   // Lots of copies!  basename's behavior is less than ideal.
-  char *c_filename = strdup(filename.c_str());
-  string base = basename(c_filename);
-  free(c_filename);
+  const char *c_filename = filename.c_str();
+  const char *p = strrchr(c_filename, '/');
+  string base = p ? p+1 : c_filename;
   return base;
 }
 
